spring:
  application:
    name: Campung-Test
    
  # 테스트용 H2 인메모리 데이터베이스 설정
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  
  # JPA/Hibernate 테스트 설정
  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트 시마다 새로 생성
    show-sql: false  # 테스트에서는 SQL 로그 끄기
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.H2Dialect
  
  # Redis 테스트 설정 (비활성화)
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 1000ms  # 빠른 타임아웃
      
  # Redis 자동 설정 비활성화 (테스트에서는 불필요)
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

# 테스트 서버 포트
server:
  port: 0  # 랜덤 포트 사용

# Swagger 비활성화 (테스트에서는 불필요)
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# 로그 레벨 설정 (테스트에서는 최소화)
logging:
  level:
    org.springframework: WARN
    com.example.Campung: INFO
